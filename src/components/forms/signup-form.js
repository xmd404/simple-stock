import React from 'react';

const formStyle = {
  minHeight: '500px',
  minWidth: '100%',
  color: '#FFF',
  backgroundColor: '#000',
};

const SignUpForm = () =>
  <div style={formStyle}>
    
  </div>

export default SignUpForm;